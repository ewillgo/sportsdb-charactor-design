package cc.sportsdb.cd.support;

import java.awt.image.BufferedImage;

public interface Texture {

    /**
     * Set object's texture
     *
     * @param texture The texture's buffer of the object
     */
    void setTexture(BufferedImage texture);
}
